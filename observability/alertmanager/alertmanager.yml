route:
  receiver: openbot
  group_by: [alertname, monitor_key]
  group_wait: 1s
  group_interval: 10s
  repeat_interval: 24h
receivers:
  - name: openbot
    webhook_configs:
      - url: http://alert-relay:4600/alertmanager
        send_resolved: false
